NebulaNV — Update Report (2025-08-11)
Scope covered today
Package: apps/user-service
Focus: Build a reliable, fast test suite (no external deps) + tidy test inventory.

What’s done
Unit tests (green)
user.service.spec.ts — create/validate/read/update profile, token helpers.

user.controller.spec.ts — route behaviors and role checks.

prisma.service.spec.ts — $connect/$disconnect lifecycle.

app.service.spec.ts — deterministic greeting via mocked ConfigService.

app.controller.spec.ts — delegates to service.

req-user.decorator.spec.ts — HTTP/RPC/fallback via exported reqUserFactory.

E2E (green, minimal)
app.e2e.spec.ts — exercises /users over HTTP with:

in-memory Prisma stub (no DB),

fake auth guard injecting req.user (no external auth),

bcrypt mocked for password change flow.

Housekeeping
Removed unused specs tied to non-existent/external guards/decorators.

Deleted redundant globals.d.ts and the old “Jest globals” canary.

Tiny source tweak: export reqUserFactory to enable direct unit testing.

Why this is stable
Tests don’t hit real gRPC, protos, or a database → deterministic and fast.

Wide, async jest.fn mocks avoid TS “never” pitfalls.

E2E validates the actual /users HTTP surface without infra coupling.

Outstanding items (outside user-service)
Auth-service ⇄ user-service gRPC timeout (carried over from 2025-07-26):

App calls time out after 5s, while grpcurl to user-service succeeds.

Likely endpoint/env wiring mismatch (localhost vs container hostname, or env not injected).

Suggested remediation (unchanged):

Standardize on USER_GRPC_URL in auth-service client; log and assert at boot.

Use user-service:50051 inside Compose, 127.0.0.1:50051 only when port is published.

Verify ClientsModule.registerAsync reads ConfigService and the var is present.

Add a one-shot connectivity probe/log around getService().

Risks / notes
None blocking in user-service.

Auth-service gRPC wiring remains the only cross-service risk to auth flows.

Next steps
(Optional) Enable coverage thresholds in jest.config.ts for user-service.

(Auth) Apply the USER_GRPC_URL cleanup + boot assertion; re-test from process.