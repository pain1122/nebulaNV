// Blog-service has no database-owned base records.
// Development demo content is created through the service's HTTP contract by
// the root `pnpm backend:seed` command.
console.log("[seed:blog-service] no base records");

export {};
