// Batch 5 adds each route controller here as it lands. Runtime and contract-only
// OpenAPI generation import this exact list so neither can silently discover a
// different public surface.
export const GATEWAY_HTTP_CONTROLLERS = [] as const;
