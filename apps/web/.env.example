# Current same-origin Next BFF. These are server-only and must match one
# enabled record in GATEWAY_APPLICATION_REGISTRY_JSON.
NEBULA_GATEWAY_API_BASE_URL=http://127.0.0.1:3002
NEBULA_PUBLIC_CLIENT_ID=admin-web-local
NEBULA_APPLICATION_ORIGIN=http://localhost:3000
