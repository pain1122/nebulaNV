{
  "name": "@nebula/grpc-auth",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "clean": "rimraf dist && rimraf src/*.js src/*.js.map src/*.d.ts",
    "build": "tsc -p tsconfig.json"
  },
  "peerDependencies": {
    "@nebula/protos": "workspace:*",
    "@nestjs/common": "^11",
    "@nestjs/core": "^11",
    "@nestjs/microservices": "^11",
    "rxjs": "^7",
    "@grpc/grpc-js": "^1"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
